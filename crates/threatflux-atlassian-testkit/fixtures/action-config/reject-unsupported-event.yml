version: 1
rules:
  - id: test
    when:
      event: pull_request
      action: opened
    extract:
      severity:
        from: issue.body
        regex: '(?mi)^severity:\s*(high)\b'
    jira:
      project_key: KAN
      issue_type: Bug
      priority_by_severity:
        high: High
      summary: test
      description: test
      dedupe:
        strategy: sha256
        fields: [issue.title]
